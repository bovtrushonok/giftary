import { Form } from '../../components/form';
import { FormWrapper } from '../../sharedViews/form';

export const LogInForm = () => {
  return (
    <FormWrapper>
      <Form />
    </FormWrapper>
  )
}
